/* =====================================================================
   Supabase keys — paste yours here. BOTH are safe to be public
   (security is enforced by Row Level Security, not by hiding the key).

   Where to find them:
     Supabase dashboard -> your project -> Project Settings -> API
       • "Project URL"        -> url
       • "anon public" key    -> anon

   Leave them empty to run the calendar in local DEMO mode (no account,
   progress saved only in the browser) — handy for previewing.
   ===================================================================== */
window.SB = {
  url:  "",   // e.g. "https://abcdefgh.supabase.co"
  anon: ""    // the long "anon public" JWT
};
